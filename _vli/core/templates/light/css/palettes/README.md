# Paletas do template `light`

O template `light` usa um visual editorial claro.

Para trocar a paleta manualmente, copie um dos arquivos desta pasta sobre `../variables.css`.

Exemplo:

```powershell
Copy-Item css/palettes/pearl.css css/variables.css -Force
```
